# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Test output
test_output/

# Temp
*.tmp
*.bak

# 4-zone skill library — TẤT CẢ tracked in git (clone xong dùng ngay, không cần sync)
# vci/ + xia/ + claudekit/ + others/ — all committed

# Sync state (regenerated each sync — chỉ dùng khi update từ upstream)
scripts/.sync-state.json
scripts/.last-audit.txt
.xia/
